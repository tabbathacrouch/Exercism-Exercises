// Should work like Array.prototype.forEach
export const each = (array, iteratee) => {};

// Use previous implementation of 'each' in your implementation of 'filter'.
export const filter = (array, predicate) => {};

// Use previous implementation of 'each' in your implementation of 'map'.
export const map = (array, transform) => {};

// Use previous implementation of 'each' in your implementation of 'reduce'.
export const reduce = (array, iteratee, accumulator) => {};

export const find = (array, predicate) => {};
